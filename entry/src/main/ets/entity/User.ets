import { NetworkUtil } from 'harmony_utils/src/main/ets/utils/NetworkUtil';

export class User {
  id: string = 'No_1060701';
  name: string = '张三';
  age: number = 20;
  addr: string = '乌市天山区';
  work: string = '工程师';
  state:string='';
  user?: User;
}